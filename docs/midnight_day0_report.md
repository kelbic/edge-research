# Midnight/Base: отчёт день-0 разведки (Фаза A пост-ФИНАЛ плана)

Дата замера: 2026-07-12 (head 48522417). План: `docs/midnight_tooling_plan.md` §2;
пороги §5 зафиксированы коммитом `7992f9f` ДО этого замера и не менялись.
Скрипт: `analysis/midnight_day0.py` (+тесты), кэш: `data/midnight_markets.json`.
Всё read-only. [P] = первоисточник (он-чейн/код/офиц. реестр).

## Вердикт по pre-registered §5: **SHADOW-АКТИВАЦИЯ** (M-T5)

- **Активация shadow** («≥1 рынок `liquidatorGate == 0` и borrow > 0»): **ДА** —
  18 из 42 рынков имеют gate=0 ∧ totalUnits>0 [P: скан+eth_call 2026-07-12].
- **Handover-порог**: НЕ достигнут — ни одного прошедшего maturity-окна с сейзабельным
  долгом ≥$5k (весь текущий borrow всех рынков ≈ $528). Считаем окна дальше (M-T5).
- **Kill-условия**: ни одно не сработало — (а) SVR/Atom-фидов НЕТ (все оракулы —
  MorphoChainlinkOracleV2-стиль поверх стандартных Chainlink-фидов, ни одного
  OEV-маркера в description/typeAndVersion); (б) уникальных ликвидаторов за первые
  окна = 2 (< 10); (в) re-deploy не обнаружен.
- **Возврат в monitoring**: часы тикают с первого gate=0 рынка (блок 48370818,
  ~2026-07-08); дедлайн правила «8 недель без окна ≥$5k» ≈ **2026-09-02**.
- Стоп-условия Фазы A (байткод ≠ эталон / 100% KYC / staging): **ни одно не сработало**
  → Фаза B (M-T1..M-T5) разрешена планом.

## 1. Верификация байткода [P] — шаг 0.1: ПОЛНОЕ СООТВЕТСТВИЕ ядра

Локальная сборка forge (solc 0.8.34, via_ir, optimizer_runs 466, evm=osaka,
bytecode_hash=none — из foundry.toml репо) vs `eth_getCode` на Base:

| Контракт | Адрес | Он-чейн | Сборка | Совпадение |
|---|---|---|---|---|
| Midnight (ядро) | `0xAdedD8ab…A18A` | 24557 B | тег `2026-07-02` (e6f2bf2) | **бит-в-бит** (24557/24557) |
| Log = midnightMempool | `0xdD6DCE32…7993` | 120 B | `336b924` | **бит-в-бит** (120/120) |
| EcrecoverRatifier | `0xd6e70365…755E` | 4139 B | e6f2bf2 | совпал по модулю immutable |
| EcrecoverAuthorizer | `0x292bEa9f…a493` | 1276 B | e6f2bf2 | совпал по модулю immutable |
| SetterRatifier | `0x800B5F12…4d63` | 2713 B | e6f2bf2 | совпал по модулю immutable |
| MidnightBundles | `0x091183d7…50A7` | 16513 B | — | **не сравнён** (см. ограничения) |

- Midnight.sol: `src/` идентичен на e6f2bf2 = 336b924 = HEAD ⇒ он-чейн механика
  (рамп/RCF/гейты/topic0) = задокументированной в `docs/morpho_v2_mechanics.md`.
  Расхождения НЕТ даже в metadata (bytecode_hash=none).
- Log при сборке тега (профиль optimizer_runs 4294967295) даёт 175 B ≠ он-чейн;
  при конфиге `336b924` («reduce mempool optimizer-runs», меняет ТОЛЬКО foundry.toml)
  — бит-в-бит ⇒ деплой собран с конфига ≥336b924 (даты сходятся: деплой 07-06 > 07-03).
- Ратификаторы: диффы только в `immutableReferences`-диапазонах artifact'а, и каждый
  дифф-сегмент = `adedd8ab…a18a` (адрес задеплоенного Midnight, immutable `MIDNIGHT`)
  — полное совпадение по модулю immutables.
- «Staging-деплой» опровергнут: код = прод-эталон, активность реальная (§3),
  адреса в официальном SDK-реестре.

## 2. Рынки [P: скан MarketCreated 48286884→48522417, 42 события]

- **42 рынка**; первый — блок 48370818 (~2026-07-08), т.е. рынки начали создавать
  через ~2 дня после деплоя контрактов.
- **`liquidatorGate == 0` на 42/42 (100%)** — все ликвидации полностью permissionless;
  `enterGate == 0` тоже на всех. Сценарий «KYC-огороженное поле» НЕ реализовался.
- `rcfThreshold = 0` на всех рынках.
- Loan-токены: USDC (33), WETH (7), EURC (2). Коллатералы: WETH, cbBTC, wstETH, USDC,
  MORPHO, WELL + обёртки `WETH-USDC-collat`/`cbBTC-USDC-collat` (lltv 0.98).
- Включённые параметры: cursors — только **0.30**; lltv ∈ {0.86, 0.915, 0.945, 0.98}
  → **maxLif/бонусы: 4.38% (lltv .86), 2.62% (.915), 1.68% (.945), 0.60% (.98)**.
- **Календарь maturity (14 уникальных, UTC)**: 07-09 00:00 ×2 (прошло), 07-10 15:00 ×3
  (прошло), 07-16 00:00 ×2, 07-17 15:00 ×8, 07-18 00:00 ×1, 07-23 00:00 ×2,
  07-31 15:00 ×9, 08-08 00:00 ×1, 08-28 15:00 ×9, 09-25 15:00 ×1, 10-30 15:00 ×1,
  11-27 15:00 ×1, 12-25 15:00 ×1, 2027-07-11 ×1. Каденс выглядит как
  еженедельные/месячные серии — календарь тикает уже сейчас.

## 3. Активность [P: все логи ядра, 43528 шт.]

Take 190, SupplyCollateral 175, Repay 43, WithdrawCollateral 87, **SetConsumed 42383**
(перекатка офферов — похоже на маркет-мейкер/апп команды), mempool (Log) 656 событий,
bundles 0. Т.е. offer-флоу живой, но **бутстрап-масштаба**: суммарный текущий borrow
по стейбл-рынкам ≈ **$528** (+2 WETH-рынка с нулём). Порог живости $10M (pre-reg dir8)
— **не пройден** и близко.

## 4. Прошедшие maturity-окна и первые ликвидации [P: 4 события Liquidate]

Все 4 — `postMaturityMode=true`, все по первому окну (maturity 2026-07-09 00:00 UTC):

| t_fill − maturity | caller | seized | repaid | badDebt |
|---|---|---|---|---|
| **+17.2 ч** | `0x6d9dea0a…` | ~0 WETH (пыль) | 0.00 USDC | 0 |
| +17.2 ч | `0x6d9dea0a…` | 0.000017 cbBTC (~$2) | 1.00 USDC | 0 |
| +17.3 ч | `0x6d9dea0a…` | ~0 cbBTC | 0.00 USDC | 0 |
| +39.0 ч | `0x6ba008e3…` | ~0 cbBTC | 0.00 USDC | 0 |

**Ключевой конкурентный факт**: рамп (0→maxLif за 60 мин) закончился за ~16 часов ДО
первого филла — **никто не играет тайминг-игру**; долг — пыль, филлы похожи на ручные/
тестовые тычки. Уникальных ликвидаторов: 2. Поле тулинга пусто и он-чейн (не только
по GitHub). Это сид shadow-лога M-T5 (paper-t* для этих окон бессмыслен: долг < газа).

## 5. Оракулы [P: eth_call probing всех 12 уникальных адресов]

- Все ценовые оракулы отвечают на `price()` и раскрывают фиды через
  BASE_FEED_1/2 / QUOTE_FEED_1/2 (MorphoChainlinkOracleV2-паттерн): ETH/USD, BTC/USD,
  USDC/USD, EUR/USD, MORPHO/USD, WELL/USD, wstETH-stETH exchange rate — стандартные
  Chainlink-описания. **Ни одного SVR/Atom/RedStone-маркера** ⇒ kill §5 не сработал.
- ⚠️ Для обёрток `*-collat` (lltv 0.98) оракулы `0x3f511770…`/`0xd8ac22e9…` не отвечают
  на фид-геттеры — тип не классифицирован день-0 зондом; разобрать в M-T2/M-T3
  (влияет на liveness-поверхность: реверт ЛЮБОГО оракула рынка блокирует liquidate).

## 6. Ограничения честности

- **MidnightBundles (16513 B) не сравнён с исходником**: контракта нет в
  `morpho-org/midnight` (e6f2bf2/HEAD), поиск по `morpho-org/sdks` (полное дерево) и
  кандидат-репо (midnight-bundles/midnight-periphery — 404) исходника не дал; в SDK
  есть только ABI. Это периферия UX-батчинга, НЕ на пути `liquidate` — ядро сверено.
- totalUnits — снапшот на head, не исторический поток; units оценены 1:1 к $ только
  для стейбл-loanToken (WETH-рынки unpriced, но там totalUnits=0, кроме пыли).
- `*-collat`-обёртки: символы получены он-чейн, их механика (protected collateral?)
  не разобрана — пункт бэклога M-T2/M-T3.
- Публичные RPC (getLogs ≤10k блоков) — скан реплеябелен, кэш датирован в data/.

## 7. Что дальше (по плану §8)

Фаза B: M-T1 (календарь — данные уже в кэше) → M-T3 (fork-harness против реального
деплоя) → M-T2 (break-even модель + exit-роутер) → M-T4 (вотчеры, каденс еженедельный)
→ M-T5 (shadow-лог, активирован этим отчётом). Live-шаги — вне плана (§5: только
решение пользователя после Gate 1 в monad-liquidator).
