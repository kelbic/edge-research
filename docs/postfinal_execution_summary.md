# Пост-ФИНАЛ: сводный отчёт исполнения (Midnight → ePBS → CPI)

Дата: 2026-07-12. Исполнение pre-registered плана (коммит `7992f9f`) по трём живым
остаткам программы. Все пороги зафиксированы ДО замеров и НЕ ослаблялись. Всё
read-only/paper; live-шаги — только отдельным решением пользователя по гейтам.
Тесты: **119 Python (зелёные)** + **8 forge (fork-harness)**.

## Фаза A — Midnight день-0 (Base): SHADOW-АКТИВАЦИЯ

Артефакты: `analysis/midnight_day0.py`+тесты, `data/midnight_markets.json`,
`docs/midnight_day0_report.md`.

- **Байткод ядра Midnight бит-в-бит** = forge-сборка тега 2026-07-02 (24557/24557 B).
  «Staging-гипотеза» опровергнута; механика = задокументированной.
- 42 рынка, `liquidatorGate == 0` на **100%** (поле не огорожено); оракулы —
  стандартный Chainlink, **SVR/Atom-маркеров нет** (kill §5 не сработал).
- 4 post-maturity ликвидации, все **пыль** (max 1 USDC); первый филл **+17ч** после
  maturity ⇒ рамп никто не играет, ботов нет. Borrow всего ~$528.
- Вердикт §5: **shadow (M-T5) активирован**; стоп-условий Фазы A нет.

## Фаза B — тулинг M-T1..M-T5: поле пусто, exit blue-chip глубокий

Артефакты: `analysis/midnight_{calendar,breakeven,watchers,shadow}.py`+тесты,
`analysis/midnight_fork_harness.t.sol`, `docs/midnight_tooling_report.md`,
`docs/midnight_mt3_forkharness_report.md`.

- **M-T1**: 14 maturity-окон / 42 рынка; ближайшее 2026-07-16.
- **M-T2 (ядро)**: exit — РЕАЛЬНЫМ Uniswap-V3-Base-роутером (не оракулом).
  Exit-импакт при maxLif vs клип: **WETH 0.06–0.26%, cbBTC 0.04–1.02%** ($10k–$1M) —
  ≪ бонуса 4.38% ⇒ окно выигрышно на любом клипе (blue-chip); MORPHO умирает к $100k;
  WELL тонкий (Uni-only — верхняя граница, Aerodrome глубже).
- **M-T3**: 8/8 forge-тестов на реальном байткоде; рамп подтверждён wei-в-wei;
  liveness-грабли реальны.
- **M-T4/M-T5**: ни один §5-триггер; поле пусто (2 ликвидатора).
- **§5**: SHADOW ✓, HANDOVER ✗ (нет окон ≥$5k), KILL ✗. **Связывающее — отсутствие
  borrow-потока, не механика и не exit.**

## Фаза C — ePBS T4/T5 (офлайн): защита mainnet-трека готова

Артефакты: `analysis/epbs_gas_model.py`+тесты (17), `analysis/epbs_sensors.py`,
`data/epbs_sensors_2026-07-12.json`, `docs/epbs_t4_report.md`.

- Газ-модель (EIP-параметры — конфиг, сверены с raw EIPs): дельты подтвердили Gate 0 —
  прямой путь **+0.0%**, 20KiB-calldata **+149.9%** (floor биндится), новый слот
  **+77 920** газа. CFI-ветвь: EIP-8038 дорожит и прямой путь (+30.3%) — **если 8038
  войдёт в SFI, инвариант «~0%» умирает** (ловит сенсор S7).
- Сенсоры S1–S7: снимок 07-12, триггеров нет; PAYLOAD_DUE_BPS=5000 (6s-дедлайн).
  T1–T3 заперты за S2 + OK пользователя.

## Фаза D — CPI vintage-бэктест: NO

Артефакты: `era5_polymarket/scripts/backtest_cpi.py` (патч nowcast) +тесты (12),
`era5_polymarket/data/nowcast_{year,month}_2026-07-12.json.gz`,
`era5_polymarket/research/cpi_vintage_report.md`.

- Живой фид Cleveland Fed (браузерный UA), снапшоты датированы/закоммичены.
- **Вердикт NO** (строго по verdict_rule): mean YoY **−0.59%** (нужно ≥+3%),
  median **−0.89%** (нужно ≥0). Диагностика упорядочена (naive ≤ vintage ≤ perfect
  по медиане), **lookahead-бага нет** (vintage далёк от perfect-потолка +15.9%),
  рынок хорошо калиброван (Brier 0.092). Направление 9 закрыто — решётка #6 (outcome-
  риск = скрытый кредит) подтверждается и на проф-сигнале.

## Рекомендация следующего шага (по приоритету плана)

1. **Midnight (приоритет 1, окно тикает)**: перевести M-T4/M-T5 в **еженедельный
   форвардный мониторинг** (дёшево, автомат). Триггеры к эскалации в monad-liquidator:
   появление реального borrow-потока (M-T4.iii Adapter готов / рост totalUnits) →
   handover-порог §5 (≥3 окна с долгом ≥$5k, exit-импакт p75 ≤2% — для WETH/cbBTC
   заведомо прошёл бы). Дедлайн возврата в monitoring ≈ **2026-09-02**. Бэклог:
   роутер на Aerodrome (уточнит mid/thin-cap exit), оракулы обёрток `*-collat`.
   **Требует решения пользователя** только на live-шаг (после Gate 1); мониторинг — нет.
2. **ePBS (приоритет 2)**: держать сенсоры S1–S7 (каденс 2 нед). Эскалация — по
   букве: установка тестнет-эпохи (S2) → старт T1–T3 после OK; вход 8038 в SFI (S7) →
   расширить T4-ветвь до основной.
3. **CPI (приоритет 3)**: **закрыть окончательно** — NO подтверждён, разморозки не
   требуется; артефакт остаётся для полноты карты (решётка #6).

Общий вывод: вердикт ФИНАЛ в силе. Ни один остаток не дал GO; Midnight — единственный
живой транзиторный опцион (blue-chip exit доказанно есть, но нет потока), операционализирован
до порога handover без единого live-шага и без ослабления критериев.
