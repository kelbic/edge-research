# Midnight live-signing бот — статус и шаги активации

Направление 8/B. Профиль: латентность+капитал расслаблены (решение пользователя
2026-07-12). Цель — заряженный бот, авто-стрельба в первую позицию ≥$5k, малый газ.

## Что уже готово (безопасно, read-only/код, в git)

- **Детектор целей** `analysis/midnight_positions.py` — энумерация заёмщиков
  (SupplyCollateral/UpdatePosition) + `debt()` per позиция; классификация
  post-maturity-сейзабельных и приближающихся окон. **Текущая цепь: 0 целей ≥$5k**
  (147 заёмщиков, всё пыль ≤$16) — ликвидировать сейчас нечего.
- **Модель входа** `analysis/midnight_breakeven.py` (M-T2) — t\* + realized-net
  РЕАЛЬНЫМ Uniswap-роутером (не оракулом).
- **Callback-контракт** `bot/MidnightLiquidator.sol` — атомарный путь
  (seize→своп через Uniswap SwapRouter02→repay), owner-gated, **гард профита**
  (реверт, если выручка < долг+minProfit). Компилируется против реальных
  интерфейсов Midnight (solc 0.8.34, osaka). **НЕ задеплоен.**
- **Мониторинг** `monitor/` — нативный VPS-cron (пн/чт 06:17 UTC), шлёт в Telegram
  прямым Bot API; эскалирует при появлении цели ≥$5k. Не зависит от сессии Claude.

## Что нужно от пользователя для боевого запуска (физические гейты, не дисциплина)

1. **Hot-key** — сгенерировать/профинансировать кошелёк (малый газ-буфер на Base).
   Ключ пользователь контролирует сам; в коде грузится из файла вне git, не логируется.
2. **Деплой** `MidnightLiquidator.sol` на Base с этого ключа (реальный газ).
3. **Валидация ДО mainnet-деплоя**: fork-replay контракта против ПЕРВОЙ реальной
   позиции ≥$5k (M-T3-стиль) — подтвердить realized-net на конкретном коллатерале
   (blue-chip exit доказанно глубокий; mid/thin-cap — риск, гард профита страхует).

## Оставшийся код (пишется по готовности, до активации)

- `bot/executor.py` — цикл: детектор → M-T2 t\* + realized-net гард → сборка
  calldata `runLiquidation` → подпись hot-key → бродкаст в t\* → мониторинг/ретрай.
- Nonce/газ-менеджмент, обработка liveness-граблей (реверт оракула/локов — из M-T3),
  анти-frontrun на Base-секвенсоре (подача к t\*).

## Последовательность

Поле пусто (0 целей), спешки нет — календарь maturity известен заранее
(ближайшее окно 07-16), монитор разбудит при появлении потока с запасом. Разумно:
дописать executor как код сейчас; деплой+фандинг+активация — когда монитор поймает
первую реальную цель, тогда же fork-replay-валидация против неё. Live-шаг — явное
решение пользователя (капитал/ключ).
