# T4-отчёт: пересчёт газ-моделей под Glamsterdam (H5) + статус T5-датчиков

Направление 6. Дата: 2026-07-12. Исполнение Фазы C по
`docs/epbs_t4t5_execution_plan.md` §1–2; pre-registration
`docs/epbs_gate0_preregistration.md` (коммит `e2a5dc38…`), критерий H5:
пересчёт обязателен при дельте ≥10% хотя бы на одном типовом паттерне.
Код: `analysis/epbs_gas_model.py` (+ тесты `analysis/test_epbs_gas_model.py`,
17/17 зелёные), датчики: `analysis/epbs_sensors.py`
(снапшот `data/epbs_sensors_2026-07-12.json`). Всё офлайн/read-only, stdlib.

> ⚠️ **РЕВИЗИЯ 2026-08-07 — читать §8 ПЕРЕД §1–§3.** Сработал датчик S7:
> скоуп Glamsterdam заморожен, EIP-2780 и 8038 вошли в SFI, условная CFI-ветвь
> стала ОСНОВНОЙ. При перепроверке чисел по первоисточникам три параметра 8038
> оказались мягче, чем считала модель 12.07, поэтому **колонка «CFI» в §1
> устарела численно**, а вывод «прямой путь ~0%» больше не описывает основную
> ветвь. Актуальная таблица, правила и адресат правил — в §8.

## 1. Таблица дельт по tx-паттернам (обе ветви)

Ветви: **SFI** = EIP-7976 + 7981 + 8037 (Scheduled for Inclusion);
**CFI** = SFI + EIP-2780 + 8038 (Considered, но уже гоняются на devnet-6/7 —
условная ветвь по дельте плана 07-12). Режим `today` — действующий mainnet
(интринсик 21k, floor EIP-7623 10/40, цены EIP-2929/2930).

| Паттерн | today | SFI | ΔSFI | CFI | ΔCFI | floor SFI | min gas limit SFI |
|---|---:|---:|---:|---:|---:|---|---:|
| liq_direct_800B (ликвидация ~300k exec) | 329,000 | 329,000 | **+0.0%** | 428,700 | +30.3% | нет | 72,200 |
| swap_800B (компактный роутинг) | 329,000 | 329,000 | +0.0% | 383,400 | +16.5% | нет | 72,200 |
| swap_5KiB (агрегаторный) | 372,200 | 372,200 | +0.0% | 426,600 | +14.6% | нет | 348,680 |
| swap_20KiB (KyberSwap/1inch-класс) | 533,000 | 1,331,720 | **+149.9%** | 1,325,720 | +148.7% | **ДА** | 1,331,720 |
| liq_new_token_slot (первый приём токена X) | 351,100 | 429,020 | **+22.2%** | 539,620 | +53.7% | нет | 72,200 |
| liq_with_al (AL 2 адреса + 4 ключа) | 328,400 | 339,152 | +3.3% | 440,052 | +34.0% | нет | 82,952 |

Сверка с pre-registered ожиданиями Gate 0 (H5):

- **прямой путь ~0%** — подтверждено: ΔSFI(liq_direct_800B) = +0.00%;
- **5KiB почти не биндится** — подтверждено: floor 327,680 < exec+calldata
  351,200 при 300k exec, ΔSFI = 0;
- **20KiB ≈ +150%** — подтверждено точно: 533,000 → 1,331,720 = **+149.9%**
  (floor 64 gas/байт биндится: 1,310,720 > 504,800);
- **новый слот +77.9k** — подтверждено точно: +77,920 gas/слот
  (20,000 → 97,920, ×4.90).

Порог H5 «≥10% хотя бы на одном паттерне» пробит уже офлайн-моделью (20KiB,
новый слот); финальное подтверждение — pre-registered реплей на публичном
тестнете Gloas (триггер S2), офлайн-числа его не заменяют.

**CFI-ветвь меняет картину качественно**: из-за EIP-8038 (STORAGE_WRITE
2,800→10,000, cold-доступы →3,000) дорожает и «прямой путь» (+30.3% на
стейт-тяжёлой ликвидации при [S]-профиле ниже), несмотря на снижение
интринсика EIP-2780 (21k→15k для вызова контракта). Если датчик S7 покажет
вход 2780/8038 в SFI — CFI-ветвь становится основной моделью, а инвариант
«прямой путь ~0%» перестаёт действовать.

## 2. Свод операционных правил для monad-liquidator

Маркер: mainnet-USDe трек (газ-модель, выбор swap-пути) пересчитать **до
активации** Glamsterdam; Base не трогать до соответствующего OP-stack-форка.

1. **(i) Роутинг**: толстые агрегаторные calldata-пути на L1 исключить из
   выбора маршрута (20KiB → +150%, floor биндится); компактный роутинг и
   5KiB-класс остаются ~0% в SFI.
2. **(ii) Пре-сид**: заранее засеять балансы/слоты эксекьютора по целевым
   токенам — первый приём токена стоит +77,920 gas/слот в SFI (+88,820 в CFI).
   Операционная дисциплина, не капитал.
3. **(iii) Дроп access-lists**: листинг ключа (3,948) дороже холодного доступа
   (2,100) на 1,848; полный чистый штраф с учётом остаточного warm-доступа
   **+1,948/ключ, +1,180/адрес** (в CFI: +2,148/ключ). Для типового AL
   (2 адреса + 4 ключа) хранение AL = +10,152 gas (+3.1% к SFI-стоимости
   ликвидации). До форка AL по-прежнему экономит ~100/ключ — переключение
   строго по активации.
4. **(iv) Headroom**: gas limit tx обязан покрывать `intrinsic + 64·байт
   calldata (+ data-цена AL)` даже когда ожидаемый gasUsed ниже — иначе tx
   невалидна. Для 20KiB минимальный лимит 1,331,720 (сегодня хватало ~533k);
   проверку добавить в подачу ДО активации.

## 3. [P]-источники (все — raw.githubusercontent.com, фетч 2026-07-12)

| Источник | Несущие числа | Статус |
|---|---|---|
| `ethereum/EIPs/…/eip-7976.md` | `TOTAL_COST_FLOOR_PER_TOKEN=16` ⇒ floor 64 gas/байт uniform (zero==nonzero); headroom-правило невалидности | [P] подтверждено |
| `ethereum/EIPs/…/eip-7981.md` | AL data cost 64 gas/байт ⇒ 1,280/адрес, 2,048/ключ поверх 2930; floor-формула с AL | [P] подтверждено |
| `ethereum/EIPs/…/eip-8037.md` | `CPSB=1530`, слот 64·1530=**97,920** (~4.9×20k), аккаунт 120·1530=**183,600** (7.34×25k) | [P] подтверждено |
| `ethereum/EIPs/…/eip-2780.md` | `TX_BASE_COST=12,000`; вызов контракта = 15,000 интринсик; декомпозированная база подставляется в floor | [P] подтверждено |
| `ethereum/EIPs/…/eip-8038.md` | COLD_ACCOUNT 2600→3000, COLD_STORAGE 2100→3000, STORAGE_WRITE 2800→10,000, AL-цены →3000/3000, STORAGE_CLEAR_REFUND →12,480 | [P] подтверждено |
| `ethereum/EIPs/…/eip-7773.md` | SFI = 10 EIP (7976/7981/8037 в SFI, 7732 в SFI); CFI: 2780/7904/8038/8045; activation-таблица пуста | [P] подтверждено |
| `consensus-specs/configs/mainnet.yaml` | `GLOAS_FORK_EPOCH=2^64−1`; `PAYLOAD_DUE_BPS=5000` (=6s), `PAYLOAD_ATTESTATION_DUE_BPS=7500`, `MIN_BUILDER_WITHDRAWABILITY_DELAY=64` | [P] подтверждено |
| `ethpandaops/glamsterdam-devnets` | devnet-6 `GLOAS_FORK_EPOCH=30` (девнет живой) | [P] подтверждено |

## 4. Расхождения плана с EIP-текстами (зафиксировано явно)

1. **«+1,848 к холодному» (план §1, AL-правило)**: по параметрам EIP-7981
   это стоимость *листинга* ключа против холодного доступа (3,948−2,100=1,848),
   без остаточного warm-доступа, который всё равно оплачивается. Полный чистый
   штраф = **+1,948/ключ** (1,900+2,048+100−2,100). Числа согласованы, разница
   методологическая; `al_rule_7981()` считает обе величины. Вывод «AL мёртв»
   не меняется.
2. **«интринсик 21k вниз» (EIP-2780)**: в интернет-версии это не единый
   срез, а декомпозиция: вызов контракта без value = 15,000; ETH-перевод
   существующему EOA остаётся 21,000; создание аккаунта переезжает в
   state-gas 183,600. В модели взято 15,000 для наших паттернов
   (вызов контракта).
3. Остальные несущие числа плана (64 gas/байт; 97.9k/183.6k; ~4.9×; +77.9k;
   +150% на 20KiB) совпали с первоисточниками точно.
4. Замечание вне модели: план §0 упоминает «devnet-7 конфиг смержен 10.07»,
   но в `network-configs/` master-ветки glamsterdam-devnets на 2026-07-12
   виден только devnet-6 — датчик S7 поймает появление каталога devnet-7.

## 5. Что НЕ удалось верифицировать по первоисточникам ([S]-допущения модели)

- **Доля нулевых байт calldata = 0.5** — допущение Gate 0 (ABI-паддинг);
  якорит today-стоимость 20KiB-паттерна = 533k из pre-registration. При
  100% ненулевых байт ΔSFI(20KiB) была бы ≈ +58%, а не +150% — чувствительность
  дельты к составу calldata существенна, реплей на тестнете обязан мерить
  фактический состав.
- **Стейт-профили exec** (ликвидация: 8 cold-аккаунтов, 25 cold-слотов,
  10 storage-обновлений; своп: 4/12/6) — синтетические оценки, влияют только
  на CFI-ветвь (8038); SFI-дельты от них не зависят (floor/слоты/AL).
- **exec_gas 300k** для свопов — предположение плана (§1: «300–400k»); floor
  5KiB (327,680) начал бы биндиться при exec ниже ~276k (327,680 − calldata
  51,200) — запас у допущения небольшой.
- Параметры 8038 помечены в самом EIP как «not yet final» (Draft) — вся
  CFI-ветвь условна по определению; числа заморожены в PARAMS-конфиге и
  правятся одной строкой.

## 6. T5: статус датчиков на 2026-07-12 (первый снапшот)

`python3 -m analysis.epbs_sensors scan` → `data/epbs_sensors_2026-07-12.json`;
все 7 источников доступны, статус OK. Сработавших триггеров **нет**:

- S1: 7732 в SFI (де-скоупа нет); SFI=10 EIP; 8282 в мета-списках отсутствует;
- S2: activation-таблица 7773 пуста; `GLOAS_FORK_EPOCH` mainnet = 2^64−1,
  в конфигах Sepolia/Hoodi/Holešky ключа Gloas нет → T1–T3 остаются запертыми;
- S3: gloas/epbs в релизах mev-boost (v1.12) и mev-boost-relay (v0.32.0) — 0
  упоминаний; rbuilder PR #855 «add builder server for epbs» open/draft
  (инженерный сигнал, не триггер);
- S4 (информационно): lighthouse v8.2.0, prysm v7.1.6, teku 26.7.1,
  nimbus-eth2 v26.6.2, grandine 2.0.5;
- S5: хэши specs/gloas/{beacon-chain,validator,builder}.md зафиксированы
  (база для диффа), PTC-стимулов/штрафов в константах не видно (PTC_SIZE,
  PAYLOAD_ATTESTATION_DUE_BPS) — предпосылки H1/H2 целы;
- S6: v1.7.0-alpha.12: PAYLOAD_DUE_BPS=5000 (payload deadline 6s — сдвиг
  «9s→6s» уже в спеке, зафиксирован как база), ATTESTATION_DUE_BPS_GLOAS=2500,
  builder withdrawability 64 эпохи;
- S7: 2780/8038/7904 все в CFI (не SFI) → CFI-ветвь T4 остаётся условной;
  devnet-6 с GLOAS_FORK_EPOCH=30 — девнеты живые, но девнет ≠ S2.

Каденс: раз в 2 недели до анонса публичного тестнета, далее еженедельно;
после установки mainnet-эпохи — ежедневно. Выход локальный, вебхуков нет.

## 7. Самоаудит (чек-лист pre-registration)

- Каждое несущее число — [P]-источник с датой фетча либо явная [S]-пометка.
- Факт отделён от интерпретации; расхождения с планом зафиксированы (§4),
  взяты числа EIP.
- Прогнозов дат нет; T1–T3 не написаны; капитал 0; наружу ничего не постится.
- Тесты: `python3 -m unittest analysis.test_epbs_gas_model` — 17/17 OK;
  `epbs_gas_model report`, `epbs_sensors scan/report` прогнаны, выводы в §1/§6.

---

## 8. Ревизия 2026-08-07: ветвь репрайсинга стала основной (триггер S7)

Дата: 2026-08-07. Повод: датчик S7 поднял триггер «репрайсинг-EIP вошёл в SFI».
Действие по плану §2 — «расширить CFI-ветвь T4 до основной». Тесты после ревизии:
`test_epbs_gas_model` 22/22, `test_epbs_monitor_noise` 27/27 (49 суммарно) — OK.

### 8.1 Проверка премиссы (у этого датчика уже было два ложных срабатывания)

Скачок SFI 10 → 19 при `cfi: []` — отпечаток, который даёт и сползание парсера
(секцию переименовали → её содержимое перетекло в соседнюю). Поэтому вердикт
вынесен по первоисточнику, а не по снапшоту:

- `raw.githubusercontent.com/.../eip-7773.md`, sha256 `90e771d9e2abe6f8…` —
  **совпадает** с sha снапшота сенсора (тот же документ, парсер читал не мимо);
- в документе **нет секции «Considered for Inclusion»** — скоуп заморожен;
  остались `EIPs Scheduled for Inclusion` (19 EIP), `Other EIPs`
  (Networking / Informational) и `Declined for Inclusion`;
- **EIP-2780 и EIP-8038 стоят прямо в списке Scheduled** — триггер настоящий;
- **EIP-7904 переведён в Informational** («Compute Gas Cost Analysis») — отдача
  сенсора `7904: absent` корректна, это не пропажа и не де-скоуп.

Сопутствующий факт: 7732 в SFI (направление не паркуется), тестнет-эпоха
по-прежнему не задана — **S2 не сработал, T1–T3 остаются закрытыми**.

### 8.2 Дрейф параметров: числа 8038 смягчились (перепроверка не косметична)

Ветвь нельзя было поднять в основные, не сверив числа заново — и правильно:

| Параметр | модель 12.07 | текст EIP 07.08 | эффект |
|---|---:|---:|---|
| `COLD_STORAGE_ACCESS` | 3,000 | **2,100** (в таблице EIP «+0%») | холодные ЧТЕНИЯ не дорожают |
| `ACCESS_LIST_ADDRESS_COST` | 3,000 | **2,900** | штраф адреса ниже |
| `ACCESS_LIST_STORAGE_KEY_COST` | 3,000 | **2,000** | штраф ключа ниже |
| `TX_VALUE_COST` (2780) | 4,244 + лог 1,756 | **6,000** одним параметром | сумма та же, форма другая |

Репрайсинг профиля ликвидации (8 холодных аккаунтов, 25 холодных слотов,
10 записей): **+74,200 gas**, а не +105,700 по июльской оценке — **на 30% мягче**.
Число запиннено тестом `test_liq_profile_repricing_is_74200`, а сам класс ошибки
(«репрайсинг тронул НЕ всё, что кажется») — тестами `TestParamPins`.

Вне профиля наших паттернов, записаны в PARAMS для полноты: `ACCOUNT_WRITE`
6,700→9,000, `CREATE_ACCESS` 7,000→12,000 (наши tx — zero-value вызовы контракта,
без CREATE и переводов ETH).

Все пять EIP по-прежнему `status: Review` — до девнет-финала числа могут
двигаться ещё раз; они и живут в `PARAMS`, а не в формулах.

### 8.3 Таблица дельт (ветвь SFI = 7976+7981+8037+2780+8038 — основная)

`partial` — историческая ветвь 12.07 без репрайсинга, оставлена для сверки.

| Паттерн | today | **SFI** | **ΔSFI** | partial | Δpartial | floor SFI | min gas limit SFI |
|---|---:|---:|---:|---:|---:|---|---:|
| liq_direct_800B | 329,000 | **397,200** | **+20.7%** | 329,000 | +0.0% | нет | 66,200 |
| swap_800B | 329,000 | 367,200 | +11.6% | 329,000 | +0.0% | нет | 66,200 |
| swap_5KiB | 372,200 | 410,400 | +10.3% | 372,200 | +0.0% | нет | 342,680 |
| swap_20KiB | 533,000 | 1,325,720 | **+148.7%** | 1,331,720 | +149.9% | **ДА** | 1,325,720 |
| liq_new_token_slot | 351,100 | 507,220 | **+44.5%** | 429,020 | +22.2% | нет | 66,200 |
| liq_with_al (2а+4к) | 328,400 | 407,952 | +24.2% | 339,152 | +3.3% | нет | 76,952 |

**Что изменилось в выводах Gate 0:**

- «прямой путь ~0%» — **больше не верно для основной ветви** (+20.7%): это было
  свойство partial. 8038 бьёт по exec-части, а не по calldata, поэтому нулевую
  дельту прямого пути форк не сохраняет;
- «20KiB ≈ +150%» — **подтверждено** (+148.7%), floor доминирует; в основной
  ветви total даже на 6,000 ниже partial за счёт подешевевшего интринсика 2780
  (21,000 → 15,000);
- «новый слот +77.9k» — **подтверждено без изменений** (8037 не двигался), но
  полная цена нового слота в основной ветви 110,020 (2,100 + 10,000 + 97,920);
- порог H5 «≥10% хотя бы на одном паттерне» пробит теперь **на всех шести**
  паттернах, а не только на толстых calldata.

### 8.4 Операционные правила и их адресат

Правила те же по существу (компактный роутинг; пре-сид слотов; дроп access-list;
headroom-проверка лимита), плюс новое из репрайсинга:

- **SSTORE в существующий слот 5,000 → 12,100 (×2.4)**: профиль ликвидации с
  10 записями дорожает на +71,000 gas. Холодные **чтения** не дорожают (2,100) —
  широкий скан позиций в одном вызове форк не наказывает; наказывает **запись**.
- Дроп access-list держится в обеих ветвях: полный штраф 2,048/ключ и
  1,280/адрес (в partial было 1,948/ключ); хранение AL 2а+4к = +10,752 gas.

**Адресата у этих правил сегодня нет — проверено, а не предположено.** Плановая
формулировка «пересчитать mainnet-USDe трек monad-liquidator» больше не
описывает реальность: `~/work/monad-liquidator` — read-only/paper (live
остановлен решением пользователя, его STATE §15), фокус закреплён на Base,
Ethereum L1 в мультичейн-скрининге отброшен (§16); USDe-трек тоже на Base
(Morpho, 2с/блок). Все боевые боты флота — на L2/альт-L1 (Base, Katana,
HyperEVM, World Chain). ⇒ Правила кладутся **чек-листом перед открытием
L1-экспозиции**. Условие расконсервации: появление любого L1-mainnet трека
ЛИБО OP-stack-форк, втягивающий эти EIP на Base.

### 8.5 Починка алертинга: защёлка устойчивых триггеров

S1/S2/S3/S7 сформулированы как свойства СОСТОЯНИЯ («репрайсинг в SFI»,
«эпоха установлена»), а не как переходы. Раз войдя, состояние истинно всегда —
S7 звонил бы в TG каждые 3 дня вечно, а S2 после установки эпохи начал бы
будить ровно в тот период, когда усталость от алертов дороже всего.

Введена защёлка по **отпечатку состояния**, породившего триггер
(`trigger_fingerprints` / `split_triggers` в `epbs_monitor.py`):

- один звонок на состояние; повтор уходит в лог строкой «ТРИГГЕР В СИЛЕ»;
- **эскалация ре-армит**: третий EIP вошёл в SFI / эпоха переехала = новый
  отпечаток = новый звонок;
- **недоступность источника защёлку не снимает** (просвет — не ре-арм):
  carry-forward восстанавливает прошлое OK-значение, отпечаток тот же;
- S5/S6 не защёлкиваются: они и так поднимаются только по изменившимся ключам.

Защёлка засеяна текущим отпечатком S7 (звонок 07.08 уже состоялся, иначе он
повторился бы на следующем проходе). Проверено сквозным прогоном с заглушкой на
транспорте TG и состоянием в tmp: то же состояние → 0 сообщений, эскалация
(7904 → SFI) → 1 сообщение, недоступный источник → 0 сообщений.

**ОТКАТ ревизии:** `git revert` коммита ревизии возвращает и модель, и защёлку;
состояние монитора до защёлки — `data/epbs_monitor_state.json.bak-pre-latch`
(достаточно удалить ключ `trigger_latch` из `data/epbs_monitor_state.json`,
чтобы вернуть прежнее поведение «звонить каждый раз»). Крон и каденс не тронуты.
